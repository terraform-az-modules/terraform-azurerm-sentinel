---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name : Terraform Azure Sentinel Module

# License of this project
license: "APACHE"

# Canonical GitHub repo
github_repo: terraform-az-modules/terraform-azurerm-sentinel

# Badges to display
badges:
  - name: "Terraform"
    image: "https://img.shields.io/badge/Terraform-v0.13-green"
    url: "https://www.terraform.io"
  - name: "Licence"
    image: "https://img.shields.io/badge/License-APACHE-blue.svg"
    url: "LICENSE.md"
  - name: "Changelog"
    image: "https://img.shields.io/badge/Changelog-blue"
    url: "CHANGELOG.md"

# Prerequesties to display
prerequesties:
  - name: Terraform
    url: https://learn.hashicorp.com/terraform/getting-started/install.html
    version: ">= 1.6.6"

providers:
  - name: azure
    url: https://azure.microsoft.com/
    version: ">= 3.116.0"

#  description of this project
description: |-
  Terraform Azure Sentinel Module is a Terraform module that allows you to create and manage Microsoft Sentinel (Azure Sentinel) resources in Azure.

# How to use this project
usage: |-
  Here are some examples of how you can use this module in your inventory structure:
  ```hcl
  module "sentinel" {
  source                     = "../../"
  name                       = "core"
  environment                = "dev"
  tenant_id                  = data.azurerm_client_config.current.tenant_id
  resource_group_name        = module.resource_group.resource_group_name
  subscription_id            = data.azurerm_client_config.current.subscription_id
  log_analytics_workspace_id = module.log-analytics.workspace_id
}
    ```
